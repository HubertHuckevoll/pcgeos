[system]
fs = EC-dos(megafile.geo)

[paths]
top = GFS:\FG
IF(ec,ini = NETEC.INI)
IF(!ec,ini = NET.INI)
sharedTokenDatabase = ..\PRIVDATA\SHARED

[gfs]
file = GFS.IMG
drive = GFS
; bootstrapPath holds the path with the
; GFS image. That path is also given as the first
; SP_TOP (see above). bootstrapPath is used
; to remove that path from SP_TOP once the GFS
; has been loaded and runs.
; bootstrapPath = C:\ENSEMBLE\FREEGEOS\60BETA
cacheFile = none

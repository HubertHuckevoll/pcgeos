/***********************************************************************
 *
 * PROJECT:       VCImpex SVG Exporter
 * FILE:          svgexpproc.goc
 *
 * DESCRIPTION:   Implements the export procedure for SVG output
 *
 ***********************************************************************/
@include <stdapp.goh>

#include "svgexp.h"
#include "vconv_ui.h"

#define VCIMPEX_SVG_GSTRING_FLAGS   (GSC_ONE)

word _pascal VCImpexSVGExportGString(GStateHandle sourceGState, VCImpexSVGExportContext *context);
word _pascal VCImpexSVGValidateGString(GStateHandle sourceGState,
                                       VCImpexSVGExportContext *context);

void _pascal VCImpexSVGResetDashPattern(VCImpexSVGExportContext *context);

word _pascal VCImpexSVGHandleLineOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word _pascal VCImpexSVGHandleRectangleOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word _pascal VCImpexSVGHandleRoundRectOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word _pascal VCImpexSVGHandleEllipseOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word _pascal VCImpexSVGHandleCurveOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word _pascal VCImpexSVGHandleArcOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word _pascal VCImpexSVGHandlePolygonOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm);
word VCImpexSVGEmitPath(GStateHandle playbackGState,
                        VCImpexSVGExportContext *context,
                        Boolean fillPath,
                        Boolean strokePath,
                        RegionFillRule fillRule,
                        const TransMatrix *tm);
void _pascal VCImpexSVGResetDashPattern(VCImpexSVGExportContext *context)
{
    if (context == NULL)
        return;

    context->dashPairCount = 0;
    context->dashSkipCount = 0;
    memset(context->dashPattern, 0, sizeof(context->dashPattern));
}

Boolean _pascal VCImpexSVGHandleStyleStateOpcode(word elementType,
                                                 VCImpexSVGExportContext *context,
                                                 word elementSize)
{
    const byte *elementData;
    word pairCount;
    word entryCount;
    word index;

    if (context == NULL)
        return FALSE;

    elementData = context->elementDataP;

    switch (elementType)
    {
        case GR_SET_LINE_STYLE:
        {
            const OpSetLineStyle *styleOp;
            word skipCount;

            if (VCImpexSVGAs(elementData, GR_SET_LINE_STYLE) == (void*)0)
            {
                return FALSE;
            }

            styleOp = (const OpSetLineStyle *)elementData;
            skipCount = (word)styleOp->OSLS_index;
            if (styleOp->OSLS_style != LS_CUSTOM)
            {
                VCImpexSVGResetDashPattern(context);
            }
            context->dashSkipCount = skipCount;
            return TRUE;
        }

        case GR_SET_CUSTOM_LINE_STYLE:
        {
            const OpSetCustomLineStyle *customOp;
            const byte *patternData;
            word skipCount;

            if (VCImpexSVGAs(elementData, GR_SET_CUSTOM_LINE_STYLE) == (void*)0)
            {
                return FALSE;
            }

            customOp = (const OpSetCustomLineStyle *)elementData;
            pairCount = customOp->OSCLS_count;
            if (pairCount > MAX_DASH_ARRAY_PAIRS)
            {
                return FALSE;
            }

            entryCount = pairCount * 2;
            if ((VCIMPEX_SVG_CUSTOM_LINE_STYLE_HEADER_SIZE +
                 (pairCount * VCIMPEX_SVG_WORD_SIZE)) != elementSize)
            {
                return FALSE;
            }

            VCImpexSVGResetDashPattern(context);
            skipCount = customOp->OSCLS_index;
            context->dashSkipCount = skipCount;
            context->dashPairCount = pairCount;
            patternData = elementData +
                          VCIMPEX_SVG_CUSTOM_LINE_STYLE_HEADER_SIZE;
            for (index = 0; index < (MAX_DASH_ARRAY_PAIRS * 2); index++)
            {
                if (index < entryCount)
                {
                    context->dashPattern[index] = (word)patternData[index];
                }
                else
                {
                    context->dashPattern[index] = 0;
                }
            }
            return TRUE;
        }

        default:
            return TRUE;
    }
}

/****************************************************************************
 * VCImpex SVG Export public entry point
 ****************************************************************************/

dword _export _pascal ExportProcedure(ExportFrame *frame)
{
    GStateHandle sourceGState;
    VCImpexSVGExportContext context;
    word error;

    EC_WARNING(VCIMPEX_WARNING_EXPORT_PROCEDURE_ENTERED);

    if (frame == NULL)
        return TE_EXPORT_ERROR;

    if (frame->EF_formatNumber != FORMAT_SVG_EXPORT)
        return TE_EXPORT_NOT_SUPPORTED;

    if (frame->EF_clipboardFormat != CIF_GRAPHICS_STRING)
        return TE_EXPORT_INVALID_CLIPBOARD_FORMAT;

    if ((frame->EF_outputFile == NullHandle) ||
        (frame->EF_transferVMFile == NullHandle) ||
        (frame->EF_transferVMChain == 0))
    {
        return TE_EXPORT_ERROR;
    }

    memset(&context, 0, sizeof(context));
    sourceGState = GrLoadGString(frame->EF_transferVMFile, GST_VMEM,
                                 VMCHAIN_GET_VM_BLOCK(frame->EF_transferVMChain));
    EC_WARNING(VCIMPEX_WARNING_LOAD_GSTRING_RETURNED);
    if (sourceGState == 0)
        return TE_METAFILE_CREATION_ERROR;

    context.bufferHeapH = MemAllocLMem(LMEM_TYPE_GENERAL, 0);
    EC_WARNING(VCIMPEX_WARNING_BUFFER_HEAP_ALLOC_RETURNED);
    if (context.bufferHeapH == NullHandle)
    {
        GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
        return TE_OUT_OF_MEMORY;
    }

    EC_WARNING(VCIMPEX_WARNING_VALIDATION_ENTERED);
    error = VCImpexSVGValidateGString(sourceGState, &context);
    EC_WARNING(VCIMPEX_WARNING_VALIDATION_RETURNED);
    if (error != TE_NO_ERROR)
    {
        MemFree(context.bufferHeapH);
        GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
        return error;
    }

    EC_WARNING(VCIMPEX_WARNING_BOUNDS_ENTERED);
    GrSetGStringPos(sourceGState, GSSPT_BEGINNING, 0);
    GrGetGStringBoundsDWord(sourceGState, 0, 0, &context.bounds);
    GrSetGStringPos(sourceGState, GSSPT_BEGINNING, 0);
    EC_WARNING(VCIMPEX_WARNING_BOUNDS_RETURNED);
    if ((context.bounds.RD_right < context.bounds.RD_left) ||
        (context.bounds.RD_bottom < context.bounds.RD_top))
    {
        MemFree(context.bufferHeapH);
        GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
        return TE_EXPORT_ERROR;
    }

    context.svgFile = frame->EF_outputFile;
    context.lineWidth = 0;
    context.lineColor = 0;
    context.fillColor = 0;
    context.lineJoin = LJ_MITERED;
    context.lineCap = LE_BUTTCAP;
    context.miterLimit = 0;
    context.lineStyle = LS_SOLID;
    VCImpexSVGResetDashPattern(&context);
    context.fillRule = WINDING;

    if (FileTruncate(context.svgFile, 0, FALSE) != 0)
    {
        MemFree(context.bufferHeapH);
        GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
        return TE_FILE_WRITE;
    }

    if (FilePos(context.svgFile, 0, FILE_POS_START) != 0)
    {
        MemFree(context.bufferHeapH);
        GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
        return TE_FILE_WRITE;
    }
    EC_WARNING(VCIMPEX_WARNING_OUTPUT_FILE_READY);

    error = VCImpexSVGInitWriter(&context);
    EC_WARNING(VCIMPEX_WARNING_INIT_WRITER_RETURNED);
    if (!error)
    {
        MemFree(context.bufferHeapH);
        GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
        return TE_FILE_WRITE;
    }

    error = VCImpexSVGExportGString(sourceGState, &context);
    EC_WARNING(VCIMPEX_WARNING_EXPORT_GSTRING_RETURNED);

    if (context.bufferHeapH != NullHandle)
    {
        EC_WARNING(VCIMPEX_WARNING_BUFFER_HEAP_FREE_ENTERED);
        MemFree(context.bufferHeapH);
        EC_WARNING(VCIMPEX_WARNING_BUFFER_HEAP_FREE_RETURNED);
        context.bufferHeapH = NullHandle;
    }

    EC_WARNING(VCIMPEX_WARNING_SOURCE_GSTRING_DESTROY_ENTERED);
    GrDestroyGString(sourceGState, 0, GSKT_LEAVE_DATA);
    EC_WARNING(VCIMPEX_WARNING_SOURCE_GSTRING_DESTROY_RETURNED);

    EC_WARNING(VCIMPEX_WARNING_EXPORT_PROCEDURE_RETURNING);
    return error;
}

/****************************************************************************
 * Validation pass
 ****************************************************************************/

word _pascal
VCImpexSVGValidateGString(GStateHandle sourceGState,
                          VCImpexSVGExportContext *context)
{
    GStringElement readType;
    VCImpexSVGOpcodeClass opcodeClass;
    word elementType;
    word elementSize;
    word error;
    word pathDepth;
    Boolean foundEnd;

    if ((sourceGState == 0) || (context == (void*)0))
        return TE_EXPORT_ERROR;

    error = TE_NO_ERROR;
    pathDepth = 0;
    foundEnd = FALSE;
    GrSetGStringPos(sourceGState, GSSPT_BEGINNING, 0);
    while (foundEnd == FALSE)
    {
        readType = GrGetGStringElement(0, sourceGState, 0,
                                       (void*)0, &elementSize);
        if ((readType == VCIMPEX_SVG_INVALID_OPCODE) ||
            (elementSize < VCIMPEX_SVG_OPCODE_SIZE))
        {
            error = TE_EXPORT_ERROR;
            break;
        }
        elementType = (word)readType;

        error = VCImpexSVGReadElement(context, 0,
                                      sourceGState, elementType);
        if (error != TE_NO_ERROR)
        {
            break;
        }

        error = VCImpexSVGValidateElement(context->elementDataP,
                                          elementType,
                                          context->elementSize);
        VCImpexSVGReleaseElement(context);
        if (error != TE_NO_ERROR)
            break;

        opcodeClass = VCImpexSVGClassifyOpcode(elementType);
        if (opcodeClass == SVG_OPCODE_UNSUPPORTED_RENDERING)
        {
            error = TE_EXPORT_NOT_SUPPORTED;
            break;
        }

        if (elementType == GR_END_GSTRING)
        {
            foundEnd = TRUE;
        }
        else if (elementType == GR_BEGIN_PATH)
        {
            if (pathDepth == 0xffff)
            {
                error = TE_EXPORT_ERROR;
                break;
            }
            pathDepth++;
        }
        else if (elementType == GR_END_PATH)
        {
            if (pathDepth == 0)
            {
                error = TE_EXPORT_ERROR;
                break;
            }
            pathDepth--;
        }

        if (foundEnd == FALSE)
        {
            GrSetGStringPos(sourceGState, GSSPT_SKIP_1, 0);
        }
    }

    VCImpexSVGReleaseElement(context);
    if ((error == TE_NO_ERROR) &&
        ((foundEnd == FALSE) || (pathDepth != 0)))
    {
        error = TE_EXPORT_ERROR;
    }

    GrSetGStringPos(sourceGState, GSSPT_BEGINNING, 0);
    return error;
}

/****************************************************************************
 * Core gstring traversal
 ****************************************************************************/

word _pascal VCImpexSVGExportGString(GStateHandle sourceGState, VCImpexSVGExportContext *context)
{
    MemHandle playbackBlockH;
    ChunkHandle playbackChunk;
    GStateHandle playbackGState;
    word elementType;
    PointWWFixed currentPosition;
    word error;
    GSRetType gsr;
    word handlerResult;
    word pathDepth = 0;
    VCImpexSVGOpcodeClass opcodeClass;
    RegionFillRule pathFillRule;
    TransMatrix tm;

    EC_WARNING(VCIMPEX_WARNING_EXPORT_GSTRING_ENTERED);

    playbackBlockH = MemAllocLMem(LMEM_TYPE_GENERAL, sizeof(LMemBlockHeader));
    EC_WARNING(VCIMPEX_WARNING_PLAYBACK_HEAP_ALLOC_RETURNED);
    if (playbackBlockH == NullHandle)
    {
        return TE_OUT_OF_MEMORY;
    }

    playbackGState = GrCreateGString(playbackBlockH, GST_CHUNK, (word*) &playbackChunk);
    EC_WARNING(VCIMPEX_WARNING_CREATE_PLAYBACK_GSTRING_RETURNED);
    if (playbackGState == 0)
    {
        MemFree(playbackBlockH);
        return TE_OUT_OF_MEMORY;
    }

    handlerResult = VCImpexSVGWriteHeader(context);
    EC_WARNING(VCIMPEX_WARNING_WRITE_HEADER_RETURNED);
    if (!handlerResult)
    {
        GrDestroyGString(playbackGState, 0, GSKT_KILL_DATA);
        return TE_FILE_WRITE;
    }

    error = TE_NO_ERROR;

    /* the main loop */
    EC_WARNING(VCIMPEX_WARNING_DRAW_LOOP_ENTERED);
    for(gsr = GrDrawGString(playbackGState, sourceGState, 0, 0, GSC_ONE, &elementType);
        gsr == GSRT_ONE;
        gsr = GrDrawGString(playbackGState, sourceGState, 0, 0, GSC_ONE, &elementType))
    {
        error = VCImpexSVGReadElement(context, playbackGState,
                                      sourceGState, elementType);
        if (error != TE_NO_ERROR)
        {
            break;
        }

        error = VCImpexSVGValidateElement(context->elementDataP,
                                          elementType,
                                          context->elementSize);
        if (error != TE_NO_ERROR)
        {
            VCImpexSVGReleaseElement(context);
            break;
        }

        opcodeClass = VCImpexSVGClassifyOpcode(elementType);
        if (opcodeClass == SVG_OPCODE_UNSUPPORTED_RENDERING)
        {
            error = TE_EXPORT_NOT_SUPPORTED;
            VCImpexSVGReleaseElement(context);
            break;
        }

        VCImpexSVGUpdateDrawingState(playbackGState, context);

        if (VCImpexSVGIsStyleStateOpcode(elementType))
        {
            if (!VCImpexSVGHandleStyleStateOpcode(elementType, context,
                                                   context->elementSize))
            {
                error = TE_EXPORT_ERROR;
                VCImpexSVGReleaseElement(context);
                break;
            }
            VCImpexSVGReleaseElement(context);
            continue;
        }

        /* Skip transform/state ops */
        if ((elementType >= GR_APPLY_ROTATION && elementType <= GR_XFORM_1F) ||
            (elementType >= GR_SAVE_STATE && elementType <= GR_ATTR_9F))
        {
            VCImpexSVGReleaseElement(context);
            continue;
        }

        switch (elementType)
        {
            case GR_BEGIN_PATH:
                if (pathDepth == 0xffff)
                {
                    error = TE_EXPORT_ERROR;
                    break;
                }
                pathDepth++;
                VCImpexSVGReleaseElement(context);
                continue;

            case GR_END_PATH:
                if (pathDepth == 0)
                {
                    error = TE_EXPORT_ERROR;
                    break;
                }
                pathDepth--;
                VCImpexSVGReleaseElement(context);
                continue;

#ifdef GR_SET_NULL_PATH
            case GR_SET_NULL_PATH:
                pathDepth = 0;
                VCImpexSVGReleaseElement(context);
                continue;
#endif

            case GR_CLOSE_SUB_PATH:
                /* nothing to emit directly for subpath closures */
                VCImpexSVGReleaseElement(context);
                continue;

            default:
                break;
        }

        if (error != TE_NO_ERROR)
        {
            VCImpexSVGReleaseElement(context);
            break;
        }

        if (pathDepth != 0)
        {
            /* suppress geometry opcodes recorded during an open path */
            VCImpexSVGReleaseElement(context);
            continue;
        }

        handlerResult = TE_NO_ERROR;
        GrGetCurPosWWFixed(playbackGState, &currentPosition);
        GrGetTransform(playbackGState, &tm);
        if (VCImpexSVGIsLineOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandleLineOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsRectangleOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandleRectangleOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsRoundRectOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandleRoundRectOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsEllipseOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandleEllipseOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsCurveOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandleCurveOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsArcOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandleArcOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsPolygonOpcode(elementType))
        {
            handlerResult = VCImpexSVGHandlePolygonOpcode(elementType, context, &currentPosition, &tm);
        }
        else if (VCImpexSVGIsPathOpcode(elementType))
        {
            pathFillRule = context->fillRule;
            if (elementType == GR_FILL_PATH)
            {
                pathFillRule = (RegionFillRule)
                    context->elementDataP[VCIMPEX_SVG_OPCODE_SIZE];
            }
            VCImpexSVGReleaseElement(context);
            handlerResult = VCImpexSVGEmitPath(
                playbackGState, context,
                (elementType == GR_FILL_PATH),
                (elementType == GR_DRAW_PATH),
                pathFillRule, &tm);
        }

        VCImpexSVGReleaseElement(context);
        if (handlerResult != TE_NO_ERROR)
        {
            error = handlerResult;
            break;
        }
    }
    EC_WARNING(VCIMPEX_WARNING_DRAW_LOOP_LEFT);

    VCImpexSVGReleaseElement(context);
    if ((error == TE_NO_ERROR) &&
        ((gsr != GSRT_COMPLETE) || (pathDepth != 0)))
    {
        error = TE_EXPORT_ERROR;
    }

    if (error == TE_NO_ERROR)
    {
        if (!VCImpexSVGWriteFooter(context))
        {
            error = TE_FILE_WRITE;
        }
    }

    EC_WARNING(VCIMPEX_WARNING_PLAYBACK_GSTRING_DESTROY_ENTERED);
    GrDestroyGString(playbackGState, 0, GSKT_KILL_DATA);
    EC_WARNING(VCIMPEX_WARNING_PLAYBACK_GSTRING_DESTROY_RETURNED);

    return error;
}

/****************************************************************************
 * Opcode handlers
 ****************************************************************************/

word _pascal
VCImpexSVGHandleLineOpcode(word elementType,
                           VCImpexSVGExportContext *context,
                           PointWWFixed *currentPosition,
                           const TransMatrix *tm)
{
    PointWWFixed startPoint;
    PointWWFixed control1;
    PointWWFixed control2;
    PointWWFixed endPoint;
    VCImpexSVGGeometryType geometryType;
    word result;

    result = VCImpexSVGDecodeGeometry(
        elementType, currentPosition,
        context->elementDataP, context->elementSize,
        &geometryType, &startPoint, &control1, &control2,
        &endPoint);
    if (result != TE_NO_ERROR)
    {
        return result;
    }
    if ((geometryType == SVG_GEOMETRY_LINE) &&
        !VCImpexSVGWriteLineElement(context, &startPoint, &endPoint, tm))
    {
        return TE_FILE_WRITE;
    }
    return TE_NO_ERROR;
}

word _pascal VCImpexSVGHandleRectangleOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm)
{
    PointWWFixed corner1;
    PointWWFixed corner3;
    Point intCorner1;
    Point intCorner3;
    Boolean filled;
    const byte *elementData;
    word offset;
    word wordValue1;
    word wordValue2;

    elementData = context->elementDataP;

    switch (elementType)
    {
        case GR_DRAW_RECT:
        case GR_FILL_RECT:
            if (VCImpexSVGAs(elementData, elementType) == (void*)0)
            {
                return TE_EXPORT_ERROR;
            }
            offset = sizeof(GStringElement);
            intCorner1.P_x = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner1.P_y = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner3.P_x = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner3.P_y = (sword)VCImpexSVGReadWord(elementData, offset);
            VCImpexSVGPointFromInt(&intCorner1, &corner1);
            VCImpexSVGPointFromInt(&intCorner3, &corner3);
            filled = (elementType == GR_FILL_RECT);
            break;

        case GR_DRAW_RECT_TO:
        case GR_FILL_RECT_TO:
            VCImpexSVGCopyPointWWFixed(&corner1, currentPosition);
            if (VCImpexSVGAs(elementData, elementType) == (void*)0)
            {
                return TE_EXPORT_ERROR;
            }
            offset = sizeof(GStringElement);
            wordValue1 = VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            wordValue2 = VCImpexSVGReadWord(elementData, offset);
            intCorner3.P_x = (sword)wordValue1;
            intCorner3.P_y = (sword)wordValue2;
            VCImpexSVGPointFromInt(&intCorner3, &corner3);
            filled = (elementType == GR_FILL_RECT_TO);
            break;

        default:
            return TE_NO_ERROR;
    }

    if (!VCImpexSVGWriteRectElement(context,
                                     &corner1,
                                     &corner3,
                                     (filled == FALSE),
                                     (filled != FALSE),
                                     filled,
                                     tm))
    {
        return TE_FILE_WRITE;
    }
    return TE_NO_ERROR;
}

word _pascal VCImpexSVGHandleEllipseOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm)
{
    WWFixed cx = {0};
    WWFixed cy = {0};
    WWFixed rx = {0};
    WWFixed ry = {0};
    sword x1, y1, x2, y2;
    sdword sum;
    sdword difference;
    Boolean filled = FALSE;
    const byte *elementData;
    word offset;

    (void)currentPosition;

    elementData = context->elementDataP;

    switch (elementType)
    {
        case GR_DRAW_ELLIPSE:
        case GR_FILL_ELLIPSE:
            if (VCImpexSVGAs(elementData, elementType) == (void*)0)
            {
                return TE_EXPORT_ERROR;
            }
            offset = sizeof(GStringElement);
            x1 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            y1 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            x2 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            y2 = (sword)VCImpexSVGReadWord(elementData, offset);

            sum = (sdword)x1 + (sdword)x2;
            VCImpexSVGSetWWFixedComponent(&cx,
                                          sum * 32768L);
            sum = (sdword)y1 + (sdword)y2;
            VCImpexSVGSetWWFixedComponent(&cy,
                                          sum * 32768L);

            difference = (sdword)x2 - (sdword)x1;
            if (difference < 0)
            {
                difference = -difference;
            }
            VCImpexSVGSetWWFixedComponent(&rx,
                                          difference * 32768L);
            difference = (sdword)y2 - (sdword)y1;
            if (difference < 0)
            {
                difference = -difference;
            }
            VCImpexSVGSetWWFixedComponent(&ry,
                                          difference * 32768L);

            filled = (elementType == GR_FILL_ELLIPSE);

            break;

        default:
            return TE_NO_ERROR;
    }

    if (!VCImpexSVGWriteEllipseElement(context,
                                       &cx,
                                       &cy,
                                       &rx,
                                       &ry,
                                       (filled == FALSE),
                                       (filled != FALSE),
                                       filled,
                                       tm))
    {
        return TE_FILE_WRITE;
    }

    return TE_NO_ERROR;
}

word _pascal
VCImpexSVGHandlePolygonOpcode(word elementType,
                              VCImpexSVGExportContext *context,
                              PointWWFixed *currentPosition,
                              const TransMatrix *tm)
{
    word pointCount;
    word index;
    Boolean closeShape;
    Boolean filledShape;
    const byte *elementData;
    word offset;
    Point intPoint;
    PointWWFixed point;
    RegionFillRule fillRule;

    (void)currentPosition;
    elementData = context->elementDataP;
    fillRule = context->fillRule;
    offset = sizeof(GStringElement);

    switch (elementType)
    {
        case GR_DRAW_POLYGON:
        case GR_DRAW_POLYLINE:
            pointCount = VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            closeShape = (elementType == GR_DRAW_POLYGON);
            filledShape = FALSE;
            break;

        case GR_FILL_POLYGON:
            pointCount = VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            fillRule = (RegionFillRule)elementData[offset++];
            closeShape = TRUE;
            filledShape = TRUE;
            break;

        default:
            return TE_NO_ERROR;
    }

    if (pointCount == 0)
    {
        return TE_NO_ERROR;
    }
    if (!VCImpexSVGWritePolygonBegin(context, closeShape))
    {
        return TE_FILE_WRITE;
    }

    for (index = 0; index < pointCount; index++)
    {
        if ((offset > context->elementSize) ||
            ((context->elementSize - offset) <
             VCIMPEX_SVG_POINT_SIZE))
        {
            return TE_EXPORT_ERROR;
        }
        intPoint.P_x = VCImpexSVGReadSword(elementData, offset);
        offset += sizeof(sword);
        intPoint.P_y = VCImpexSVGReadSword(elementData, offset);
        offset += sizeof(sword);
        VCImpexSVGPointFromInt(&intPoint, &point);
        if (!VCImpexSVGWritePolygonPoint(context, &point))
        {
            return TE_FILE_WRITE;
        }
    }

    if (!VCImpexSVGWritePolygonEnd(context, filledShape, fillRule, tm))
    {
        return TE_FILE_WRITE;
    }
    return TE_NO_ERROR;
}

word VCImpexSVGEmitPath(GStateHandle playbackGState,
                        VCImpexSVGExportContext *context,
                        Boolean fillPath,
                        Boolean strokePath,
                        RegionFillRule fillRule,
                        const TransMatrix *tm)
{
    MemHandle pathMem;
    void *pathPtr;
    GStateHandle pathSource;
    MemHandle pathPlaybackBlock;
    GStateHandle pathPlayback;
    ChunkHandle pathPlaybackChunk;
    VCImpexSVGPathBuilder builder;
    GSRetType gsr;
    word elementType;
    PointWWFixed currentPosition;
    PointWWFixed startPoint;
    PointWWFixed control1;
    PointWWFixed control2;
    PointWWFixed endPoint;
    VCImpexSVGGeometryType geometryType;
    word result;
    VCImpexSVGOpcodeClass opcodeClass;

    pathMem = NullHandle;
    pathPtr = (void*)0;
    pathSource = 0;
    pathPlaybackBlock = NullHandle;
    pathPlayback = 0;
    memset(&builder, 0, sizeof(builder));
    result = TE_NO_ERROR;

    if ((context == (void*)0) || (playbackGState == 0))
    {
        return TE_EXPORT_ERROR;
    }

    pathMem = GrGetPath(playbackGState, GPT_CURRENT);
    if (pathMem == NullHandle)
    {
        return TE_NO_ERROR;
    }

    pathPtr = MemLock(pathMem);
    if (pathPtr == (void*)0)
    {
        result = TE_OUT_OF_MEMORY;
        goto cleanup;
    }

    pathSource = GrLoadGString(PtrToSegment(pathPtr), GST_PTR,
                               PtrToOffset(pathPtr));
    if (pathSource == 0)
    {
        result = TE_OUT_OF_MEMORY;
        goto cleanup;
    }

    pathPlaybackBlock =
        MemAllocLMem(LMEM_TYPE_GENERAL, sizeof(LMemBlockHeader));
    if (pathPlaybackBlock == NullHandle)
    {
        result = TE_OUT_OF_MEMORY;
        goto cleanup;
    }

    pathPlayback = GrCreateGString(pathPlaybackBlock, GST_CHUNK,
                                   (word*)&pathPlaybackChunk);
    if (pathPlayback == 0)
    {
        result = TE_OUT_OF_MEMORY;
        goto cleanup;
    }
    pathPlaybackBlock = NullHandle;

    for (gsr = GrDrawGString(pathPlayback, pathSource, 0, 0,
                             VCIMPEX_SVG_GSTRING_FLAGS, &elementType);
         gsr == GSRT_ONE;
         gsr = GrDrawGString(pathPlayback, pathSource, 0, 0,
                             VCIMPEX_SVG_GSTRING_FLAGS, &elementType))
    {
        result = VCImpexSVGReadElement(context, pathPlayback,
                                       pathSource, elementType);
        if (result != TE_NO_ERROR)
        {
            break;
        }

        result = VCImpexSVGValidateElement(context->elementDataP,
                                           elementType,
                                           context->elementSize);
        if (result != TE_NO_ERROR)
        {
            VCImpexSVGReleaseElement(context);
            break;
        }

        opcodeClass = VCImpexSVGClassifyOpcode(elementType);
        if (opcodeClass == SVG_OPCODE_UNSUPPORTED_RENDERING)
        {
            result = TE_EXPORT_NOT_SUPPORTED;
            VCImpexSVGReleaseElement(context);
            break;
        }

        GrGetCurPosWWFixed(pathPlayback, &currentPosition);
        result = VCImpexSVGDecodeGeometry(
            elementType, &currentPosition,
            context->elementDataP, context->elementSize,
            &geometryType, &startPoint, &control1, &control2,
            &endPoint);
        VCImpexSVGReleaseElement(context);
        if (result != TE_NO_ERROR)
        {
            break;
        }

        switch (geometryType)
        {
            case SVG_GEOMETRY_MOVE:
                if (!VCImpexSVGPathBuilderMoveTo(context, &builder,
                                                 &endPoint))
                {
                    result = TE_FILE_WRITE;
                }
                break;

            case SVG_GEOMETRY_LINE:
                if (!VCImpexSVGPathBuilderLineTo(context, &builder,
                                                 &startPoint, &endPoint))
                {
                    result = TE_FILE_WRITE;
                }
                break;

            case SVG_GEOMETRY_CURVE:
                if (!VCImpexSVGPathBuilderCurveTo(
                        context, &builder, &startPoint,
                        &control1, &control2, &endPoint))
                {
                    result = TE_FILE_WRITE;
                }
                break;

            case SVG_GEOMETRY_CLOSE:
                if (!VCImpexSVGPathBuilderClose(context, &builder))
                {
                    result = TE_FILE_WRITE;
                }
                break;

            default:
                if (opcodeClass == SVG_OPCODE_SUPPORTED_RENDERING)
                {
                    result = TE_EXPORT_NOT_SUPPORTED;
                }
                break;
        }

        if (result != TE_NO_ERROR)
        {
            break;
        }
    }

    VCImpexSVGReleaseElement(context);
    if ((result == TE_NO_ERROR) && (gsr != GSRT_COMPLETE))
    {
        result = TE_EXPORT_ERROR;
    }
    if ((result == TE_NO_ERROR) && (builder.hasData != FALSE) &&
        !VCImpexSVGWritePathEnd(context, fillPath, strokePath, fillRule,
                               tm))
    {
        result = TE_FILE_WRITE;
    }

cleanup:
    VCImpexSVGReleaseElement(context);
    if (pathPlayback != 0)
    {
        GrDestroyGString(pathPlayback, 0, GSKT_KILL_DATA);
    }
    if (pathPlaybackBlock != NullHandle)
    {
        MemFree(pathPlaybackBlock);
    }
    if (pathSource != 0)
    {
        GrDestroyGString(pathSource, 0, GSKT_LEAVE_DATA);
    }
    if (pathPtr != (void*)0)
    {
        MemUnlock(pathMem);
    }
    if (pathMem != NullHandle)
    {
        MemFree(pathMem);
    }

    return result;
}

word _pascal VCImpexSVGHandleRoundRectOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm)
{
    PointWWFixed corner1;
    PointWWFixed corner3;
    Boolean filled = FALSE;
    Point intCorner1;
    Point intCorner3;
    word radiusValue;
    const byte *elementData;
    word offset;
    word wordValue1;
    word wordValue2;

    elementData = context->elementDataP;

    switch (elementType)
    {
        case GR_DRAW_ROUND_RECT:
        case GR_FILL_ROUND_RECT:
            if (VCImpexSVGAs(elementData, elementType) == (void*)0)
            {
                return TE_EXPORT_ERROR;
            }
            offset = sizeof(GStringElement);
            radiusValue = VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner1.P_x = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner1.P_y = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner3.P_x = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            intCorner3.P_y = (sword)VCImpexSVGReadWord(elementData, offset);
            VCImpexSVGPointFromInt(&intCorner1, &corner1);
            VCImpexSVGPointFromInt(&intCorner3, &corner3);
            filled = (elementType == GR_FILL_ROUND_RECT);
            break;

        case GR_DRAW_ROUND_RECT_TO:
        case GR_FILL_ROUND_RECT_TO:
            VCImpexSVGCopyPointWWFixed(&corner1, currentPosition);
            if (VCImpexSVGAs(elementData, elementType) == (void*)0)
            {
                return TE_EXPORT_ERROR;
            }
            offset = sizeof(GStringElement);
            radiusValue = VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            wordValue1 = VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            wordValue2 = VCImpexSVGReadWord(elementData, offset);
            intCorner3.P_x = (sword)wordValue1;
            intCorner3.P_y = (sword)wordValue2;
            VCImpexSVGPointFromInt(&intCorner3, &corner3);
            filled = (elementType == GR_FILL_ROUND_RECT_TO);
            break;

        default:
            return TE_NO_ERROR;
    }

    if (!VCImpexSVGWriteRoundRectElement(context,
                                         &corner1,
                                         &corner3,
                                         radiusValue,
                                         radiusValue,
                                         (filled == FALSE),
                                         (filled != FALSE),
                                         filled,
                                         tm))
    {
        return TE_FILE_WRITE;
    }
    return TE_NO_ERROR;
}

word _pascal
VCImpexSVGHandleCurveOpcode(word elementType,
                            VCImpexSVGExportContext *context,
                            PointWWFixed *currentPosition,
                            const TransMatrix *tm)
{
    PointWWFixed startPoint;
    PointWWFixed control1;
    PointWWFixed control2;
    PointWWFixed endPoint;
    VCImpexSVGGeometryType geometryType;
    word result;

    result = VCImpexSVGDecodeGeometry(
        elementType, currentPosition,
        context->elementDataP, context->elementSize,
        &geometryType, &startPoint, &control1, &control2,
        &endPoint);
    if (result != TE_NO_ERROR)
    {
        return result;
    }
    if ((geometryType == SVG_GEOMETRY_CURVE) &&
        !VCImpexSVGWriteCubicPathElement(context, &startPoint,
                                         &control1, &control2,
                                         &endPoint, tm))
    {
        return TE_FILE_WRITE;
    }
    return TE_NO_ERROR;
}

word _pascal VCImpexSVGHandleArcOpcode(word elementType, VCImpexSVGExportContext *context, PointWWFixed *currentPosition, const TransMatrix *tm)
{
    Boolean fill = FALSE;
    WWFixed cx;
    WWFixed cy;
    WWFixed rx;
    WWFixed ry;
    sword startAngle;
    sword endAngle;
    sword x1;
    sword y1;
    sword x2;
    sword y2;
    ArcCloseType closeType;
    WWFixedAsDWord cxFixed;
    WWFixedAsDWord cyFixed;
    WWFixedAsDWord rxFixed;
    WWFixedAsDWord ryFixed;
    const byte *elementData;
    word offset;
    word wordValue;
    sdword sum;
    sdword difference;

    (void)currentPosition;

    elementData = context->elementDataP;

    switch (elementType)
    {
        case GR_DRAW_ARC:
        case GR_FILL_ARC:
        {
            if (VCImpexSVGAs(elementData, elementType) == (void*)0)
            {
                return TE_EXPORT_ERROR;
            }
            offset = sizeof(GStringElement);
            wordValue = VCImpexSVGReadWord(elementData, offset);
            closeType = (ArcCloseType)wordValue;
            offset += sizeof(word);
            x1 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            y1 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            x2 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            y2 = (sword)VCImpexSVGReadWord(elementData, offset);
            offset += sizeof(word);
            startAngle = VCImpexSVGReadSword(elementData, offset);
            offset += sizeof(word);
            endAngle = VCImpexSVGReadSword(elementData, offset);

            sum = (sdword)x1 + (sdword)x2;
            VCImpexSVGSetWWFixedComponent(&cx, sum * 32768L);
            sum = (sdword)y1 + (sdword)y2;
            VCImpexSVGSetWWFixedComponent(&cy, sum * 32768L);
            difference = (sdword)x2 - (sdword)x1;
            if (difference < 0)
            {
                difference = -difference;
            }
            VCImpexSVGSetWWFixedComponent(&rx,
                                          difference * 32768L);
            difference = (sdword)y2 - (sdword)y1;
            if (difference < 0)
            {
                difference = -difference;
            }
            VCImpexSVGSetWWFixedComponent(&ry,
                                          difference * 32768L);

            cxFixed = VCImpexSVGPackWWFixed(&cx);
            cyFixed = VCImpexSVGPackWWFixed(&cy);
            rxFixed = VCImpexSVGPackWWFixed(&rx);
            ryFixed = VCImpexSVGPackWWFixed(&ry);

            fill = (elementType == GR_FILL_ARC) ? TRUE : FALSE;
            if (!VCImpexSVGWriteArcElement(
                    context, cxFixed, cyFixed, rxFixed, ryFixed,
                    startAngle, endAngle, closeType, fill, tm))
            {
                return TE_FILE_WRITE;
            }
            return TE_NO_ERROR;
        }

        default:
            break;
    }
    return TE_NO_ERROR;
}

word _pascal VCImpexSVGReadWord(const byte *data, word offset)
{
    word value;

    if (data == NULL)
    {
        return 0;
    }

    memcpy(&value, data + offset, sizeof(word));
    return value;
}

sword _pascal VCImpexSVGReadSword(const byte *data, word offset)
{
    sword value;

    if (data == NULL)
    {
        return 0;
    }

    memcpy(&value, data + offset, sizeof(sword));
    return value;
}

void _pascal VCImpexSVGReadWWFixed(const byte *data, word offset, WWFixed *value)
{
    if ((data == NULL) || (value == NULL))
    {
        return;
    }

    memcpy(value, data + offset, sizeof(WWFixed));
}

void _pascal VCImpexSVGSetWWFixedComponent(WWFixed *component, WWFixedAsDWord value)
{
    component->WWF_int = (word)((((sdword)value) >> 16) & 0xffff);
    component->WWF_frac = (word)(value & 0xffff);
}

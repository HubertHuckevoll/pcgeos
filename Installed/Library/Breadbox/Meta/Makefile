#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = meta
CGM             = cgm.h cgm.goc cgmraw.goc
HPGL            = hpgl.goc
META            = meta.goc
SPLINEOP        = splineop.goc
SVG             = svg.h dbglog.h svg.goc svgGeom.goc svgShape.goc\
                  svgStyle.goc svgUtil.goc svgView.goc svgTransform.goc\
                  svgPath.goc
UI_TO_RDFS      =
OBJS            = cgm.obj cgmraw.obj hpgl.obj meta.obj splineop.obj svg.obj\
                  svgGeom.obj svgShape.obj svgStyle.obj svgUtil.obj\
                  svgView.obj svgTransform.obj svgPath.obj
COMMON          = metaint.h
MODULES         =
CMODULES        = CGM HPGL meta splineop SVG
SRCS            = $(CGM) $(HPGL) $(META) $(SPLINEOP) $(SVG) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = meta

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
